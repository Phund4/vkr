### Лабораторная работа: моделирование, эксперименты и анализ ошибок

**Тема**: Проверка пригодности готовых моделей для задач ИТС в контуре проекта.  
**ФИО**: Попов Александр Иванович  
**Группа**: БВТ2203

---

## Шаг 1. Подготовка данных

Подготовка выполнена на основе исходной архитектурной документации и стратегии валидации из `validation-scaling.md`.

### 1.1. Загрузка и предобработка

- Источник данных: видеоконтур ИТС.
- Для экспериментов использованы:
  - `ml-experiments/data/CCTV-accidents/val` — **валидация** трека `accident` (классы `Accident` / `Non Accident`);
  - `ml-experiments/data/accident/test` — **тест** трека `accident` (та же схема папок по классам);
  - `ml-experiments/data/congestion/.../labels/test.csv` — **тест** трека `congestion` (регрессия).
- Предобработка соответствует инференсу:
  - ресайз до входного размера чекпойнта;
  - преобразование в тензор и нормализация.

### 1.2. Разделение val/test (без train)

Обучение в отчёте не выполняется; каталог **`data/train` не используется** (нигде в скриптах `ml-experiments` он не создаётся и не ожидается).

- **Валидация (accident):** `CCTV-accidents/val` — выбор победителя по моделям (`winners.json` для трека accident).
- **Тест (accident):** `accident/test` — итоговые метрики в отчёте и графиках для accident.
- **Тест (congestion):** выборка из `video_ground_truth` / `labels/test.csv` (целевой показатель согласно сценарию, по умолчанию 0.0).
- Разбиение по независимым единицам (видео/сессии), а не по случайным кадрам одной записи, сохраняется на уровне подготовки датасетов.

### 1.3. Feature engineering

Feature engineering в классическом смысле **не применялся**, чтобы не менять входное пространство модели. Используются только преобразования, совместимые с инференсом.

### 1.4. Фиксация в коде

Предобработка и оценка зафиксированы в скриптах:
- `ml-experiments/scripts/run_pipeline.sh`
- `ml-experiments/scripts/evaluate_video_gt.py`
- `ml-experiments/inference_core.py`

---

## Шаг 2. Выбор и реализация baseline-модели

Поскольку обучение baseline запрещено условиями работы, baseline трактуется как **самая простая/лёгкая из готовых моделей**.

- Для трека `accident`: baseline = `baseline_cnn` (сравнение с `resnet18`).
- Для трека `congestion`: baseline = `tiny_cnn` (сравнение с `linear_resnet`).

Обучение в рамках отчёта не описывается; запускались только инференс-прогоны `best.pt` с вычислением метрик.

---

## Шаг 3. Настройка экспериментального пайплайна

Логирование экспериментов организовано через артефакты в `ml-experiments/benchmark/`:

- машиночитаемый отчёт: `video_gt_results.json`;
- человекочитаемый отчёт: `video_gt_report.md`;
- графики сравнения (PNG).

Фиксируются:
- трек/модель/чекпойнт;
- метрики качества;
- latency (mean/p50/p95);
- throughput (fps);
- победитель по каждому треку.

---

## Шаг 4. Проведение экспериментов

Проведена серия сравнений готовых моделей (без изменения весов):

- `accident`: `baseline_cnn` vs `resnet18`;
- `congestion`: `tiny_cnn` vs `linear_resnet`.

### 4.1. Результаты — accident (валидация: `CCTV-accidents/val`; тест: `accident/test`)

| модель | accuracy | precision_crash | recall_crash | f1_crash | f1_normal | mean_P_crash | p95_P_crash | fps |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| baseline_cnn | 0.4700 | 0.4700 | 1.0000 | 0.6395 | 0.0000 | 0.5272 | 0.5294 | 63.45 |
| resnet18 | 0.6700 | 0.6750 | 0.5745 | 0.6207 | 0.7080 | 0.4871 | 0.7071 | 10.24 |

Задержки:

- baseline_cnn: среднее `15.76 мс`, p95 `18.33 мс`
- resnet18: среднее `97.70 мс`, p95 `110.59 мс`

### 4.2. Результаты — congestion (целевое значение = 0.0)

| модель | mae | rmse | fps |
|---|---:|---:|---:|
| tiny_cnn | 0.1800 | 0.1834 | 53.89 |
| linear_resnet | 0.1976 | 0.1995 | 8.83 |

Задержки:

- tiny_cnn: среднее `18.56 мс`, p95 `30.79 мс`
- linear_resnet: среднее `113.27 мс`, p95 `127.73 мс`

---

## Шаг 5. Анализ ошибок

### 5.1. Выявленные слабые места

- Для `accident` наблюдается компромисс между классами: `baseline_cnn` даёт высокий `recall_crash=1.0`, но полностью проваливает `normal` (`f1_normal=0.0`), а `resnet18` более сбалансирован по классам.
- У тяжёлых моделей (`resnet18`, `linear_resnet`) значительно хуже latency/fps, что создаёт риск срыва SLA при потоке камер.

### 5.2. Причины

- Наборы `CCTV-accidents/val` и `accident/test` могут быть ограничены по размеру, поэтому метрики чувствительны к смещению распределения сцен.
- Возможен сдвиг домена между данными, на которых обучались чекпойнты, и текущим видео из контура.

### 5.3. Способы улучшения (без реализации в рамках данного документа)

- Расширить тестовый accident-набор кадрами с подтверждёнными инцидентами.
- Провести калибровку порогов и постобработки для снижения доли ложных срабатываний.
- Добавить контроль по сегментам/камерам для диагностики доменного сдвига.

---

## Шаг 6. Выбор финальной модели

Финальный выбор для инференса:
- `accident` -> `resnet18`
- `congestion` -> `tiny_cnn`

Обоснование выбора:

1. **Качество по accident:** `resnet18` даёт более сбалансированное качество между `crash` и `normal` (лучший итоговый баланс классов), в отличие от `baseline_cnn`, который уходит в перекос на один класс.
2. **Качество по congestion:** `tiny_cnn` лучше по `MAE/RMSE` и остаётся значительно быстрее альтернативы.
3. **Компромисс сложность vs качество:** для accident выбран более тяжёлый, но более устойчивый по классам вариант; для congestion — лёгкая и точная модель.
4. **Соответствие нефункциональным требованиям:** обе выбранные модели укладываются в практически применимый latency-профиль для текущего контура.

Формат сохранения модели для инференса: `best.pt` (PyTorch checkpoint), пригодный для загрузки в `ml-serving`.
